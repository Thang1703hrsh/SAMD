# Core runtime deps
# Versions recorded from the environment used to produce paper results.
# Use `pip install -r requirements.txt` to reproduce exactly.

numpy==1.26.4
pandas==2.2.2
scipy==1.13.0
scikit-learn==1.4.2
tqdm==4.66.4

# Deep learning
torch==2.1.2
transformers==4.40.2
accelerate==0.30.1
peft==0.11.1

# Token / string distance utilities
python-Levenshtein==0.25.1
editdistance==0.8.1

# Notebook runtime
jupyterlab==4.1.8
ipykernel==6.29.4
nbformat==5.10.4

# Misc
kagglehub==0.2.9

# LLM2Vec (optional — only needed for Mistral-based teacher experiments)
llm2vec==0.1.4

# Pinned for protobuf/transformers compatibility
protobuf==3.20.3
